# Local JSON "DB"

This folder simulates a database for development.
- `local-db.json` holds structured transcript data and planner state.
- This will be replaced with Postgres once GCP Cloud SQL is set up.

Do not store raw PDFs here.
